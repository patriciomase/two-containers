import React from "react";

const Requeriments = () => {
  return (
    <section style={{ margin: "10px", marginBottom: "40px" }}>
      <h1>React Practical Exercise 1</h1>
      <p>
        Given the following layout build the necessary logic to make the
        elements to switch from one container to the other when clicking on each
        one of them.
      </p>
    </section>
  );
};

export default Requeriments;
